# TitaKita 部署装前需知

> 给准备自部署一个 TitaKita 节点的人。**安装本身只要几分钟**，但下面的账号/服务请**先备齐再开装**——按"办理耗时"从长到短排序，越靠前的越早去办，免得装到一半被卡住。
>
> 基于开源项目 [Hi.Events](https://hi.events)（AGPL-3.0）。

---

## 0. 先想清楚：你要"试用"还是"正式上线"？

**🅰️ 只想先试一试（体验模式）—— 零外部账号，5 分钟搞定**

什么 Stripe、邮件、域名都**不需要**。直接：

```bash
cd docker/all-in-one
./setup.sh && docker compose up -d
# 打开 http://localhost:8123/auth/register 注册体验
```

- 邮件不会真发，全部写进容器日志（方便你看长什么样）
- 不收钱（支付关闭）
- 适合：先摸清产品、给自己看看、做演示

**🅱️ 正式上线给真实用户用** —— 才需要下面第 1~5 项。请往下看。

---

## 1. 上线要备齐的账号（按办理耗时排序）

### ⏱️ 第一优先：Stripe 收款账号（要等审核，现在就去办）

- **作用**：收票款。没有它就只能办免费活动。
- **怎么办**：注册 [stripe.com](https://stripe.com) → 填身份/公司信息、银行账户，可能需营业证明 → 等审核。
- **⚠️ 先确认你所在国家/地区支持 Stripe**（部分地区不支持，[查支持国家](https://stripe.com/global)）。
- **产出**：`Publishable Key`（pk_...）+ `Secret Key`（sk_...），上线后填进 `.env` 的 `STRIPE_PUBLIC_KEY` / `STRIPE_SECRET_KEY`。
- **耗时**：审核可能数小时到数天，所以排第一。

### 📧 第二优先：邮件发送服务（要配 DNS，急不来）

- **作用**：发确认邮件、电子票、提醒。没配好邮件，用户收不到票。
- **选一个**：[Amazon SES](https://aws.amazon.com/ses/) / [Postmark](https://postmarkapp.com) / [SendGrid](https://sendgrid.com) / [Mailgun](https://mailgun.com)（小量基本免费~约 $15/月）。
- **要做**：注册 + 在你的域名加 **SPF / DKIM** 解析记录（DNS 生效要等一会儿，所以排前面）。
- **产出**：SMTP 主机/端口/用户名/密码，填进 `.env` 的 `MAIL_*`。

### 🌐 第三优先：云服务器 + 域名（随时可办）

- **服务器**：跑 TitaKita 的机器。推荐 **[Hetzner](https://www.hetzner.com/cloud)**（便宜稳定，约 €4/月起）；也可用 DigitalOcean / Vultr / Railway / Render。
  - 最低配置：**~2GB 内存 / 10GB 硬盘 / 1–2 核**。
  - 装好 Docker 即可（[Docker 安装](https://docs.docker.com/engine/install/)）。
- **域名**（强烈建议）：约 $10–15/年。绑到服务器，配 HTTPS。没域名也能用 IP，但邮件送达率和体验都差。

### 🗄️ 第四：对象存储（可上线后再配）

- **作用**：存活动封面图、PDF 电子票。不配的话本地磁盘也能用，但重建容器会丢。
- **推荐**：[Cloudflare R2](https://www.cloudflare.com/products/r2/)（有免费额度）或 AWS S3。
- **产出**：bucket 名 + access key，填进 `.env` 的 `AWS_*` 并把 `FILESYSTEM_*_DISK` 切到 `s3-public`/`s3-private`。

### ⚖️ 第五：你自己的隐私政策 + 服务条款（上线前必备）

- **作用**：在你的节点买票的人，数据是**你**收集的、款是走**你**的 Stripe——所以隐私政策/服务条款必须是**你自己的**（不是 Hi.Events 的，也不是 TitaKita 总部的）。**法律要求 + Stripe 强制要求**。
- **怎么办**：起步可用免费生成器（Termly / iubenda / GetTerms 等）拿到现成网址；规模化前建议律师过一遍。
- **产出**：两个网址，填进 `.env` 的 `VITE_PRIVACY_URL` / `VITE_TOS_URL`。**留空则页面不显示这两个链接**（不会错误地指向第三方）。

---

## 2. 装前必须知道的"不内置"

- **WhatsApp / 短信通知**：本版**没有**，只发邮件。需要的话属另行定制，且需自备 Twilio / Meta 企业账号。
- **去品牌**：页脚的 "Powered by Hi.Events" 受 AGPL 第 7(b) 条约束需保留；要完全去除需购买 Hi.Events [商业授权](https://hi.events/pricing)（设 `VITE_I_HAVE_PURCHASED_A_LICENCE=true`）。

---

## 3. 开装前自检清单

正式上线前，确认你已备齐：

- [ ] **Stripe 双 Key**（已过审）—— 要收钱才需要
- [ ] **邮件 SMTP 账密**（域名 DNS 的 SPF/DKIM 已配）
- [ ] **云服务器**（推荐 Hetzner，已装 Docker）
- [ ] **域名**（已解析到服务器 IP）
- [ ] **对象存储 key**（或先用本地磁盘）
- [ ] **自己的隐私政策 + 服务条款网址**（填 `VITE_PRIVACY_URL` / `VITE_TOS_URL`）
- [ ] 知悉 WhatsApp 等通知**不内置**
- [ ] 已决定页脚署名：保留 / 购授权去除

> 只想体验？以上**全部可跳过**，见上面第 0 节 🅰️。

---

## 4. 费用速览

| 项目 | 费用 |
|---|---|
| 一次性 | ~$0（软件开源免费）|
| 服务器（Hetzner 等）| 约 **$5–25/月** |
| 邮件服务 | 约 **$0–15/月**（小量常免费）|
| 对象存储（R2）| 免费额度内 **$0** |
| 域名 | 约 **$10–15/年** |
| Stripe | 无月费，**按交易抽成**（约 2.9% + $0.30/笔，依地区）|

**合计：每月约 $10–40 + 交易抽成。**

---

## 5. 部署步骤

账号备齐后，参见 [`docker/all-in-one/README.md`](docker/all-in-one/README.md) 的「Going to production」一节：在服务器上 `./setup.sh` → 填好 `.env` 的域名/Stripe/邮件 → `docker compose up -d` → 配 HTTPS 反代（推荐 Caddy 自动证书）。
